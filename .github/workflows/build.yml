name: build

on:
  push:
    branches: [ main, 2.0.0-alpha ]
  pull_request:
    # Branch filters match the *base* of a pull request. Without 2.0.0-alpha here
    # the 2.0 integration branch and everything stacked onto it would merge with
    # no CI at all.
    branches: [ main, 2.0.0-alpha ]

permissions:
  contents: read

jobs:
  test:
    strategy:
      matrix:
        ruby-version: ['3.2', '3.3', '3.4']
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby-version }}
          bundler-cache: true

      - name: Install Dependency
        run: |
          bundle install

      - name: Run tests
        run: bundle exec rspec

      - name: Upload coverage to Codecov
        if: matrix.ruby-version == '3.4'
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage/lcov.info
          fail_ci_if_error: true
